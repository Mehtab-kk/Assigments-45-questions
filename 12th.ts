//Greetings: Start with the array you used in Exercise 11, but instead of just 
//printing each person’s name, print a message to them. The text of each message
//should be the same, but each message should be personalized with the person’s name.
// (task  12)
let newNames:string[]=['Mehtab','Dilsher','Dildar','Mustafa']
newNames.forEach(name=>{  //uses for each method
    console.log(`Dear, ${name} have a great day!`);
    
})